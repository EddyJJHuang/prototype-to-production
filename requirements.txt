openai>=1.30.0
pandas>=2.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0
pdfplumber>=0.11.0
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Optional — only needed for --ocr flag (scanned/image PDFs):
# pdf2image>=1.17.0
# pytesseract>=0.3.10
# System deps for OCR (macOS): brew install tesseract poppler
